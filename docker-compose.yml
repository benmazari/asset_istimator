version: '3.8'

services:
  web:
    build: .
    container_name: amortissement_web
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
      - ./config.yaml:/app/config.yaml
    environment:
      - TZ=Africa/Algiers
